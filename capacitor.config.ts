import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.stater',
  appName: 'ionic-firebase-authentication',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
